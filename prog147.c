#include<stdio.h>
int main()
{
    int no;
    printf("\n Enter Two number:");
    scanf("%d",&no);
    if(no>=0)
        printf("\n The number is positive:%d",no);
    else
        printf("\n The number is negatie: %d",no);
}
