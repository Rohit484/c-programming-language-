#include<stdio.h>
void main()
{
    int no;
    printf("\n Enter the number: ");
    scanf("%d",&no);
    if(no==100)
        printf("\n You Enter the 100 value: %d",no);
    else
        printf("\n You Enter the value is another: %d",no);
}
